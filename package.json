{
  "name": "icarus-mobile",
  "version": "0.0.1",
  "description": "Icarus Mobile - Capacitor, React Native, and Expo replacement.",
  "main": "index.js",
  "scripts": {
    "build": "node scripts/build.js",
    "start": "live-server www",
    "deploy:ios": "npm run build && cd ios/IcarusMobileApp && xcodebuild -workspace IcarusMobileApp.xcworkspace -scheme IcarusMobileApp -sdk iphonesimulator -configuration Debug",
    "deploy:android": "npm run build && cd android/app && ./gradlew assembleDebug"
  },
  "dependencies": {},
  "devDependencies": {
    "live-server": "^1.2.1"
  }
}
